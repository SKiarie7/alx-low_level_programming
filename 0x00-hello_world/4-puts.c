#include <stdio.h>

/*
 * main - prints the required phrase
 * Return: 0 if exited properly, otherwise return a non-zero integer
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return(0)
}
